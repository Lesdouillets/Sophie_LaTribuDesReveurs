/**
 * Drive.gs — réservé à l'écriture dans la fiche client Excel (Google Sheet
 * par famille) une fois le mécanisme de correspondance ligne ↔ fiche défini
 * avec Sophie. La génération de Google Doc a été retirée (non souhaitée).
 */
